{
  "name": "birdgame",
  "version": "1.0.0",
  "description": "Browser 3D bird flying game with multiplayer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node build.js",
    "build:prod": "node build.js --prod",
    "start:prod": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
